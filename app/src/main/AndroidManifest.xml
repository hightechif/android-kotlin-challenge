<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fadhil.challenge">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission
        android:name="android.permission.CAMERA"
        android:maxSdkVersion="24" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Challenge"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.movies.MovieDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.heroes.HeroDetailActivity"
            android:exported="true" />
        <activity
            android:name=".ui.student.StudentActivity"
            android:exported="true"
            android:label="Student Data Management" />
        <activity
            android:name=".ui.movies.MoviesActivity"
            android:exported="true"
            android:label="Movies App" />
        <activity
            android:name=".ui.heroes.HeroesActivity"
            android:exported="true"
            android:label="Heroes App" />
        <activity
            android:name=".ui.myview.MyViewActivity"
            android:exported="true"
            android:label="My View App" />
        <activity
            android:name=".ui.barcalculator.BarCalculatorActivity"
            android:exported="true"
            android:label="Bar Volume Calculator" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="Challenge App by Fadhil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>